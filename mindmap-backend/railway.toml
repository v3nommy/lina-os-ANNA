[build]
builder = "NIXPACKS"

[deploy]
startCommand = "bash init_db.sh && gunicorn server:app --bind 0.0.0.0:$PORT --timeout 120 --workers 2"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
